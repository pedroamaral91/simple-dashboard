import styled from 'styled-components';
import { default as ButtonTemplate } from '@components/buttons/button-circle-icon/index';

export const Container = styled.div``;

export const ButtonCircleIcon = styled(ButtonTemplate)`
  place-self: center;
`;
